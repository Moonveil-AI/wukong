# ⚠️ NOTE: This example uses the .env file from the WORKSPACE ROOT
#
# The server.ts file automatically loads ../../.env (workspace root)
# You don't need to create a .env file in this directory!
#
# If you don't have a .env in the workspace root yet:
#   1. Go to workspace root: cd ../..
#   2. Copy template: cp env.example .env
#   3. Edit and add your API keys
#
# Required variables in root .env:
#   ANTHROPIC_API_KEY=sk-ant-...  (or GEMINI_API_KEY or OPENAI_API_KEY)
#
# Optional variables:
#   PORT=3001
#   HOST=localhost
#   DATABASE_PATH=./examples/ui/data/wukong-ui.db

